# Energy Guidance for Single-Step Generative Models

基于 *Generative Modeling via Drifting*（arXiv 2602.04770）的官方实现，探索在单步生成模型上施加能量引导的两种方法，并对比其效果与局限性。

---

## 背景

Drifting 是一种**单步**生成模型：给定类别标签 $c$，模型 $f$ 将随机噪声 $\varepsilon \sim \mathcal{N}(0, I)$ 一次性映射到图像 $x$：

$$x = f(\varepsilon, c)$$

推理无需 ODE/SDE 求解器，仅需一次前向传播（1 NFE）。

我们希望引入一个**能量函数** $E(x)$ 来引导生成结果向某个偏好方向倾斜（$E$ 越小越好），同时尽量保持图像质量。为此测试两种最基础的方案。

---

## 实验设置

### 模型

| 项目 | 配置 |
|------|------|
| 模型 | `pixel_B_sota`（ViT-B/16，像素空间） |
| 架构 | LightningDiT，hidden=768，depth=12，heads=12 |
| 输入/输出 | $\varepsilon \in \mathbb{R}^{256\times256\times3}$，$x \in [-1,1]^{256\times256\times3}$ |
| 条件化 | 类别嵌入 + CFG scale + 32维噪声坐标嵌入（64类） |
| NFE | 1（单步） |

### 能量函数

初步实验使用**颜色偏好**作为代理能量，以便定量验证引导方向是否正确：

$$E_{\text{red}}(x) = -\,\mathbb{E}\bigl[R - \tfrac{1}{2}G - \tfrac{1}{2}B\bigr]$$

最小化 $E$ 即最大化红色通道相对于其他通道的优势，使图像偏红。类似地定义 $E_{\text{blue}}$、$E_{\text{green}}$、$E_{\text{bright}}$、$E_{\text{dark}}$。

---

## 方法

### Method 1：直接输出引导

对生成结果 $x$ 直接做一步梯度下降：

$$x' = \operatorname{clip}\!\left(x - \alpha_1 \cdot \frac{\nabla_x E(x)}{\|\nabla_x E(x)\|_\infty},\; -1,\; 1\right)$$

- **梯度归一化**采用 L∞ 范数，使 $\alpha_1$ 的含义直观：**受影响最强的像素通道恰好偏移 $\alpha_1$**。对于像 $E_{\text{red}}$ 这类在空间上均匀的能量函数，每个红色通道像素都会精确偏移 $\alpha_1$。
- **计算量极低**：不需要额外的模型前向传播。
- **本质**：在 $x$ 上做像素级别的线性叠加，与模型的生成过程完全解耦。

### Method 2：噪声空间引导

通过整个模型反向传播，在输入噪声 $\varepsilon$ 上做梯度下降，再重新生成：

$$\varepsilon_{t+1} = \varepsilon_t - \alpha_2 \cdot \frac{\nabla_\varepsilon E\bigl(f(\varepsilon_t, c)\bigr)}{\|\nabla_\varepsilon E\bigl(f(\varepsilon_t, c)\bigr)\|_2}, \qquad t = 0, \ldots, T-1$$

$$x' = f(\varepsilon_T, c)$$

- **梯度归一化**采用 L2 范数，在 $\varepsilon$ 高维空间中保持各维度更新均衡。
- **计算量**：每步需要一次前向 + 一次反向传播（穿过全部 12 层 transformer），共 $T$ 步。实现上用 `jax.lax.scan` 将循环编译为单一 XLA 程序。
- **本质**：通过模型的逆过程隐式地将 $\varepsilon$ 推向"生成偏红图像的噪声区域"，最终生成图像仍由模型完整输出。

### 关键区别

```
Method 1:  ε  →  f  →  x  →  x - α·∇_x E(x)       (后处理，跳过模型)
Method 2:  ε  →  ε - α·∇_ε E(f(ε))  →  f  →  x'    (从噪声重新生成)
```

---

## 实验结果

### 定量结果

以 `--energy red --alpha-m1 0.5 --alpha 0.3 --steps 5` 为例，三类图像的能量变化：

| 类别 | 方法 | R | G | B | $E$ | $\Delta E$ |
|------|------|---|---|---|-----|-----------|
| class 95 (jacamar) | baseline | 0.305 | 0.475 | 0.743 | +0.6085 | — |
| | m1-output | **0.547** | 0.351 | 0.620 | −0.1234 | **−0.732** |
| | m2-noise | 0.327 | 0.470 | 0.711 | +0.5269 | −0.082 |
| class 22 (bald\_eagle) | baseline | 0.465 | 0.511 | 0.548 | +0.1282 | — |
| | m1-output | **0.713** | 0.403 | 0.444 | −0.5797 | **−0.708** |
| | m2-noise | 0.450 | 0.490 | 0.524 | +0.1132 | −0.015 |
| class 88 (macaw) | baseline | 0.376 | 0.296 | 0.274 | −0.1813 | — |
| | m1-output | **0.611** | 0.182 | 0.174 | −0.8657 | **−0.685** |
| | m2-noise | 0.385 | 0.301 | 0.267 | −0.2022 | −0.021 |

**观察**：
- Method 1 在所有类别上能量下降约 **0.68–0.73**，R 通道均值提升约 **+0.15–+0.25**，效果显著且一致。
- Method 2 在同等 $\alpha$ 下能量下降仅约 **0.02–0.08**，约为 Method 1 的 **1/10 到 1/40**。

---

### 定性分析

**Method 1 的效果**

以 jacamar（本身为蓝色调）为例，施加 $\alpha_1 = 0.5$ 的红色引导后，R 通道从 0.305 升至 0.547（+0.242），B 通道从 0.743 降至 0.620（−0.123）。

这种变化对应的是**像素级线性叠加**：原始图像每个 R 像素 +0.5，G 像素 −0.25，B 像素 −0.25（经 clip）。图像会呈现出明显的红色色调，但因为这是在 $x$ 上的均匀偏移，图像结构本身不变——整体效果类似于在摄影后期对整张图做色温调整。

**Method 2 的效果**

同样的 jacamar 图像，$\alpha_2 = 0.3$、5 步引导后，R 通道仅从 0.305 升至 0.327（+0.022）。变化微弱，但图像仍然是完整的自然图像。

这揭示了一个核心现象：**模型的先验分布对引导形成了阻力**。

---

## 核心发现：模型先验的"阻力效应"

Method 2 能量下降弱的根本原因不是梯度步长不够大，而是模型在学习过程中形成了一个**有结构的分布**——对于每个类别，噪声到图像的映射高度非线性，且图像质量的分布是"紧密的"。

在噪声空间做梯度下降时，梯度方向会指向"生成更红图像的噪声区域"，但这个区域在噪声空间中可能：

1. **远离当前 $\varepsilon$**，需要极大步长才能到达；
2. **被模型映射回自然图像分布**，即使 $\varepsilon$ 发生了较大偏移，最终生成的 $x$ 仍贴近自然图像，因为模型会"修正"偏离。

这正是噪声空间引导的价值所在，也是其局限所在：

| 维度 | Method 1 | Method 2 |
|------|----------|----------|
| 能量下降幅度 | 大（可精确控制） | 小（受模型先验限制） |
| 图像自然度 | 降低（偏离流形） | 保持（在流形上） |
| 图像语义一致性 | 保持（仅色调变化） | 保持（结构完整） |
| 计算成本 | 极低（0 次额外前向） | 高（$T$ 次前向+反向） |

---

## 归一化方式的讨论

两种方法选用了不同的归一化策略，这一选择本身值得记录。

**为什么 Method 1 用 L∞？**

$E_{\text{red}}$ 的梯度在每个像素上是常数：$\partial E / \partial R_i = -1/(H \cdot W)$。若使用 L2 归一化，则：

$$\|\nabla_x E\|_2 = \frac{\sqrt{1.5}}{\sqrt{H \cdot W}} \approx \frac{1.22}{256} \approx 0.00476$$

此时 $\alpha = 0.3$ 对应的每像素偏移约为 $0.3 \times (1/(H \cdot W)) / 0.00476 \approx 0.001$，完全不可见。用 L∞ 归一化后，$\alpha$ 直接等于最大像素偏移量，物理含义清晰。

**为什么 Method 2 用 L2？**

噪声 $\varepsilon$ 的梯度由模型雅可比决定，各分量大小不均，存在极端值。L∞ 归一化会让某个维度主导更新，导致不稳定。L2 归一化在高维空间中更为均衡，且与随机梯度下降的标准实践一致。

---

## 结论

1. **Method 1（直接输出引导）**：效果强、计算廉价，但本质是后处理，生成结果脱离了模型学到的图像流形。对于颜色这类低阶全局统计量，结果看起来像是"涂了滤镜"，图像结构不受影响，但也因此缺乏与语义内容的交互——模型对"什么样的鸟应该有什么颜色"的理解没有被利用。

2. **Method 2（噪声空间引导）**：能量下降弱，但结果仍在自然图像流形上。其弱效应反映的正是 Drifting 模型的一个特性：生成映射 $f$ 非常"稳健"，小的噪声扰动被高度压缩，不容易通过修改 $\varepsilon$ 来大幅改变输出的全局统计量。

3. **单步生成模型的引导困境**：两种方法暴露了单步模型在能量引导上的内在张力。扩散模型/Flow Matching 在每一步都可以注入引导，而单步模型只有"前"（噪声）和"后"（输出）两个注入点，缺少中间层次的引导机会。Method 1 和 Method 2 分别代表这两个极端：前者完全绕开模型，后者完全依赖模型的可微性。

**潜在改进方向**：

- 对 Method 2 使用更大的 $\alpha$ 和更多步数（但会加速偏离 $\mathcal{N}(0,I)$ 先验）；
- 在 $\varepsilon$ 上加 Langevin 噪声，在引导同时保持对先验的贴近（类似 DPS/DDRM 在扩散模型中的做法）；
- 在中间特征层（如 transformer 的 key/value）上施加引导，而非在 $\varepsilon$ 或 $x$ 的端点；
- 将 $E$ 定义为更高层次的语义特征（如 CLIP 相似度），而非像素统计量。
